export const EVALUATION_INSTRUCTIONS = [
  'MODE: EVALUATION',
  '',
  'Goal: produce a fair, structured critique or assessment of the subject the user asked about.',
  '',
  'MANDATORY RULES:',
  '1. Be objective: list both strengths and weaknesses.',
  '2. Base every claim on information already present in the conversation context or the provided source material.',
  '3. Do not add external facts, opinions, or invented details that are not grounded in the context.',
  '4. Keep every field a plain string, number, or array as described below. No markdown, no HTML tags, no code fences in string values.',
  '5. Empty or unavailable values must be "" or [] — never null or undefined.',
  '',
  'Required fields:',
  '- category: a short label such as Evaluation, Review, Assessment, Comparison.',
  '- title: a concise, descriptive headline.',
  '- subtitle: an optional one-line summary (empty string if not needed).',
  '- subject: the item, idea, answer, or choice being evaluated.',
  '- verdict: a concise overall conclusion (e.g. "Recommended", "Mixed", "Not recommended").',
  '- score: a numeric rating such as 1–10 or 1–5. Use a number, not a string.',
  '- scoreLabel: a human-readable label for the score (e.g. "8/10", "Good", "Strong").',
  '',
  'Optional fields:',
  '- reasoning: a short paragraph explaining the score and verdict.',
  '- strengths: an array of 0–5 positive points. Each entry MUST be an object with exactly one key: "text".',
  '- weaknesses: an array of 0–5 critical points. Each entry MUST be an object with exactly one key: "text".',
  '- recommendations: an array of 0–5 actionable suggestions. Each entry MUST be an object with exactly one key: "text".',
  '- sources: an array of source objects with url and title. Only include real sources from previous tool results; otherwise use an empty array.',
  '',
  'Optional media fields (use only when online research returns real images or videos):',
  '- heroImageUrl: the single best image URL related to the evaluation subject.',
  '- heroImageAlt: a short accessibility description for the hero image.',
  '- heroCaption: an optional caption for the hero image.',
  '- heroVideoUrl: the single best video URL related to the evaluation subject. Prefer heroVideoUrl over heroImageUrl when both are available. Only use URLs from supported providers: YouTube, Vimeo, Dailymotion, Loom, Wistia, or direct video files.',
  '- heroVideoCaption: an optional caption for the hero video.',
  '- galleryTitle: a short title for an image gallery (e.g., "Gallery").',
  '- galleryItems: an array of image objects when multiple relevant images are available. Each entry must include imageUrl and may include imageAlt, title, and caption. If online research returns 3 or more image URLs, galleryItems MUST contain at least 3 items. Only use imageSearch URLs (720p minimum); never use low-resolution news thumbnails.',
  '- videoGalleryTitle: a short title for a video gallery.',
  '- videoGalleryItems: an array of video objects when multiple relevant videos are available. Each entry must include videoUrl and may include title and caption. If online research returns 3 or more video URLs, videoGalleryItems MUST contain at least 3 items. Only use supported providers (YouTube, Vimeo, Dailymotion, Loom, Wistia) or direct video files. Respect videoTargetCount from the tool context.',
  '',
  'Rules:',
  '- Write the entire response in the same language as the latest user message.',
  '- Never emit placeholder text such as "undefined", "null", "none", or "n/a". Use an empty string or empty array instead.',
  '- Only include media URLs that come from tool results. Never fabricate image or video URLs.',
  '- When selecting images, prefer 2560×1440 (1440p). 1280×720 (720p) is the enforced minimum; never use images below that resolution.',
  '- IMAGE DOMAIN RESTRICTION: only use image URLs from trusted sources. Reject Google thumbnail proxies (encrypted-tbn*.gstatic.com, t*.gstatic.com), data URIs, localhost, private IPs, and unknown hosts without a direct image file extension.',
  '- Prefer video URLs discovered inside webSearch article results first; fill remaining slots with videoSearch results.',
  '- Respect imageTargetCount and videoTargetCount from the tool context. Do not exceed those targets.',
  '',
  'FINAL REMINDER:',
  '- Be fair and balanced. Support the verdict with concrete strengths, weaknesses, and reasoning drawn from the context.',
].join('\n');

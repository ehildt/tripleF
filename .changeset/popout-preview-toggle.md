---
"@triplef.io/dashboard": patch
---

- SysCtl popout preview is now a persistent toggle (stays visible until dismissed) instead of a timed 3s preview; switching tabs dismisses it
- Popup geometry uses a symmetric edge inset for anchored positions; preview button and video popout panel updated

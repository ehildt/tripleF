---
"dashboard": minor
---
- REST panel (RestPanel.vue, RestPanel.RequestPanel.vue) and use-rest-panel composable removed entirely
- "rest" tab renamed to "http" across the app: stores, helpers, components, tests
- App store simplified: restRequestId/mcpRequestId merged into single requestId, single socket provider instead of separate REST/MCP providers
- App header tabs simplified: separate REST/MCP counts merged into single api-count
- EventLog: unified message handling, removed per-tab message slot differentiation
- MCP panel expanded: McpPanel.RequestPanel grew from 176 lines, new methods added
- use-mcp-panel composable significantly expanded (146 lines) with new methods and spec
- Added ProtocolField input component
- DebugPanel: Details and Header.Menu expanded with new layout/controls
- DebugPanel.Health simplified, removed unused sections
- DebugPanel.Results simplified with inline filtering
- DLQ panels: removed unused event/room references, simplified socket subscription logic
- DLQ PayloadEditor expanded with new features
- Socket subscription composable cleaned up

---
"dashboard": minor
---
- Introduced session-based architecture: new session store (319 lines) with createNewSession, addExchange, setActiveSession, renameSession, deleteSession, markExchangeDone, compactExchanges
- ChatPanel v1 (Conversations, Input, Messages, use-chat, chat store) introduced then immediately removed in favor of session architecture
- EventLog Exchange component created: role-based message display (user/assistant) with retry support
- EventLog SessionHeader component: editable title, token usage display (promptEvalCount/evalCount), compact/shrink controls
- EventLog restructured: removed slot-based architecture, added Exchange and SessionHeader sub-components
- MCP ConversationsPanel added (172 lines) then refined and simplified
- PromptSection shared component extracted from DlqPromptSection
- FileUpload styling improved (FileUpload.Files.File)
- EventLog auto-scroll composable improved
- Chat tab added to and removed from header in rapid iteration
- Server: visions processor minor updates for session support

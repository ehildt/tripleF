---
"dashboard": minor
---
- Toast colors refactored in all themes: hardcoded hex values replaced with CSS custom property references (e.g. var(--color-status-success))
- All theme CSS comment headers removed (accent, status, method, color descriptions)
- Diablo theme removed; 5 new themes added: baldursgate, residentevil, wuchang, wukong, yakuza
- Style.css refactored: added root-accent-gradient and bg-grid utility classes, body background split into separate classes, panel-glow renamed
- DebugPanel health tiles redesigned with new layout and color classes
- DebugPanel details panel refactored with restructured tag display and metadata sections
- TabButton redesigned, DetailTag component updated with new color scheme
- DebugPanel Header Menu: disabled state for HTTP/CLR buttons when counts are zero
- Debug store: new debug.helper.ts extracted, detail-field-colors helper refactored
- Theme store enhanced: darkThemes array replaced with configurable themeColors/darkThemes maps
- Version injection via Vite define (VITE_APP_VERSION) with version.ts module
- AppThemeSwitcher spec updated for new theme set
- Panel header actions restructured for DLQ panels
- Package upgrades (lucide-vue-next, eslint, etc.)
- cspell, README, and tsconfig configuration cleanups

---
"@triplef.io/dashboard": patch
---

- Playlist panel now detects whether the current conversation contains videos so the docked player's empty state and hints are reachable even with an empty queue
- New `conversationHasVideos` helper with spec; floating media bar and playlist panel wiring updated

---
"dashboard": patch
"server": minor
---
- DLQ query: added 30-second auto-refetch polling interval
- DLQ store: fetchDlqCount now fetches all pages (paginated with PAGE_SIZE=200) for complete known-IDs tracking
- DLQ list body: switched from manual watch-based sorting to computed property
- DLQ entry selection: simplified read marking — marks entry as read on selection regardless of previous state
- DLQ store: known IDs now tracked with full page fetches, readTracker.pruneMissing called to clean stale entries
- Session store: session type support (temporary vs persistent), with UI toggle (Pin/PinOff icons)
- Session store: compacting state flag (LoaderCircle spinner when active, Shrink icon when idle)
- SessionHeader: session type indicator (T/P badge), pin/unpin toggle button, compact button with loading state
- Session: compactExchanges method with server round-trip
- EventLog: done marking moved after content assignment to prevent duplicate exchanges
- ChatToolbar: temporary checkbox in conversation creation dialog, session type badge in conversation list
- Server: new compact controller (compact.controller.ts), compact processor (compact.processor.ts), compact service (compact.service.ts)
- Server: compact DTOs with request/response models, compact endpoint registered in main module
- Server: JSON-RPC controller and service enhanced with compact support
- Server: new vision constants module, updated prompt constants for compact
- Server: BullMQ constants added for compact queue
- Server: visions processor wired to compact support
- DLQ FiltersPanel: search icon moved to InputText prepend-icon slot

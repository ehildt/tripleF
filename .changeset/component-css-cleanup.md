---
"dashboard": minor
---
- animations.css deleted (429 lines): all harmony-flow, harmony-pulse, harmony-shift, border-flow, text-glow, tab-solid-activate keyframes and utilities removed
- Legacy button components removed: ActionButton (spec + vue), ClearButton (spec + vue), FilterButton (spec + vue), BaseButton (spec + vue), ActionTagButton (spec + vue)
- Legacy composables removed: use-debounced-loading (spec + ts), use-event-log (spec + helper + ts)
- Legacy input components removed: JsonField, MethodField, ProtocolField, SubmitButton (specs + vue + index)
- Legacy EventLog components removed: EventLog.Header (122 lines), EventLog.Item (72 lines + spec) — functionality merged into ChatToolbar and Exchange
- Legacy DLQ components removed: DlqCheckbox, DlqFailureBlock, DlqSourceToggle (specs + vue)
- MCP panel removed entirely: McpPanel, McpPanel.RequestPanel, McpPanel.ConversationsPanel, use-mcp-panel (spec + ts), use-mcp.mutation (type + ts), PreprocessingPanel, preprocessing store
- EventLog ChatToolbar component created (489 lines then extracted to dedicated file): model selection, task selection, numCtx, stream toggle, conversation management, file upload, subscription management
- use-submit composable introduced (304 lines): centralized form submission with socket provider, event/room connection, arguments persistence, file upload, streaming support
- App store simplified: removed preprocessing, MCP references; tab colors renamed (tab-mcp→tab-accent)
- All 14 theme CSS files: tab-mcp renamed to tab-accent
- AppMainContent restructured: removed per-tab event log slots, simplified grid layout, added ChatToolbar in collapsed left panel
- EventLog simplified to shell component, delegating rendering to Exchange/SessionHeader/ChatToolbar
- SessionHeader: debug details expanded, markdown rendering helper added, collapsible sections
- DebugPanel details enhanced with select/expand controls
- Ollama config adapter/service refactored, prompt constants reorganized
- PromptSection component extracted as shared component
- Visions processor simplified, Ollama config removed
- Method colors helper refactored, tag colors helper updates
- DLQ store: added socket event tracking
- Style.css: cleaned up and new markdown content styles added

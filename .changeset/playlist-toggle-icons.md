---
"@triplef.io/dashboard": patch
---

- Replaced the `ListCheck` icon with `ListMinus` on the playlist add/remove toggle across all media surfaces (floating media bar, video list items, hero media, video gallery items) to better communicate the "remove from playlist" state

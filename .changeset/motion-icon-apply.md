---
"@triplef.io/dashboard": patch
---

- `MotionIcon` applied across UI components: theme selector, nav menu, prompt action bar, model selector, new-conversation/stream-settings menus, DLQ action/reload buttons, floating media bar, and preview button

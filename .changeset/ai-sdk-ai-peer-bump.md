---
'@triplef/ai-sdk': patch
---

Bump the `ai` peer dependency to `^7.0.83` to align with the server's `ai@7.0.83` (and the `@triplef/agent` peer bump), keeping the `Tool`/`ToolSet` types from `@ai-sdk/provider-utils@5.0.32` consistent across the workspace.
